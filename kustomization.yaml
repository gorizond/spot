apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: spot-system

resources:
  - deployment.yaml

configMapGenerator:
  - name: spot-node-label-config
    files:
      - node_label_config.py
      - servo_driver.py
      - spot_cli.py
